name: tenant-service
base: bare
build-base: ubuntu@22.04
version: 0.1.0
summary: Canonical Identity Platform Hook Service
description: >
  This is the Canonical Identity Platform Hook Service used for handling Hydra
  Hooks and managing groups
license: Apache-2.0
run-user: _daemon_
platforms:
  amd64: null
services:
  tenant-service:
    override: replace
    command: /usr/bin/tenant-service serve
    startup: enabled
parts:
  go-build:
    plugin: go
    source: .
    source-type: local
    build-snaps:
      - go/1.25/stable
    build-packages:
      - make
      - git
    override-build: |
      make build
      install -D -m755 ./app ${CRAFT_PART_INSTALL}/opt/tenant-service/bin/app
    organize:
      opt/tenant-service/bin/app: usr/bin/tenant-service
    stage-packages:
      - base-files_var
      - base-files_chisel
  deb-security-manifest:
    plugin: make
    source: https://github.com/canonical/rocks-security-manifest
    source-type: git
    source-branch: main
    override-prime: gen_manifest
